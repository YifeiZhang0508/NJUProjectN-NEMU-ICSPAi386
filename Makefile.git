STU_ID = icspa_mooc_student

# DO NOT modify the following code!!!
GITFLAGS = -q --author='tracer-pa-public <tracer@njuics.org>' --no-verify --allow-empty

# prototype: git_commit(msg)
# Auto-commit disabled - only version control your own changes
define git_commit
	@echo "Skipping auto-commit for: $(1)"
endef
